---
    import links from '../data/links.ts';
    import { PowerIcon } from '@heroicons/react/24/outline';
---
<script>
</script>
<div class="flex h-full flex-col px-3 py-4 md:px-2">
  <a class="mb-2 flex h-20 items-end justify-start rounded-md bg-blue-600 p-4 md:h-40" href="/">
   <div class="w-32 text-white md:w-40">
      <!-- Logo or Branding -->
    </div>
  </a>

  <div class="flex grow flex-row justify-between space-x-2 md:flex-col md:space-x-0 md:space-y-2">
      {links.map((link) => {
        const LinkIcon = link.icon;
        return (
        <ul>
            <li>
                <a href={link.link} class="flex items-center p-3 rounded-md text-gray-900 hover:bg-gray-200">
                    <span class="flex self-center p-0 w-8 md:w-10 md:p-2"><LinkIcon /></span>
                    <span class="text-lg px-2 hidden md:flex">{link.label}</span>
                </a>
            </li>
        </ul>
        );
      })}
    </ul>
    
    <div class="hidden h-auto w-full grow rounded-md bg-gray-50 md:block">
        <form>
        <button class="flex h-[48px] w-full items-center justify-center gap-2 rounded-md bg-gray-50 p-3 text-sm font-medium hover:bg-sky-100 hover:text-blue-600 md:flex-none md:justify-start md:p-2 md:px-3">
            <span class="w-10 p-2"><PowerIcon /></span>
            <div class="flex items-center text-lg px-2 rounded-md text-gray-900 hover:bg-gray-200">Sign Out</div>
        </button>
        </form>
    </div>
  </div>
  
</div>